name: Docker checks

on: [push]

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Docker Compose Build
      env:
        SHA: ${{ github.sha }}
      run: docker-compose up -d --build